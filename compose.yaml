services:
  
  server:
    build:
      context: .
      dockerfile: server.Dockerfile
    network_mode: host

  client:
    build:
      context: .
      dockerfile: client.Dockerfile
    network_mode: host

