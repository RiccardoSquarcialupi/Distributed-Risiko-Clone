services:
  
  server:
    build:
      context: .
      dockerfile: server.Dockerfile
    ports:
      - "8000:5000"

  client:
    build:
      context: .
      dockerfile: client.Dockerfile
    ports:
      - "6080:6080"
      - "5900:5900"
    